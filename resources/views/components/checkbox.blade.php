<div class="form-check form-check-primary">
    <input class="form-check-input" type="checkbox" value="1" id="{{ $id }}" name="{{ $id }}"
        {{ $checked }}>
    <label class="form-check-label" for="{{ $id }}">{{ $label }}{!! $extra !!}</label>
</div>
